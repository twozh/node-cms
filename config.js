var config = {
	mongoPath: "mongodb://localhost/node-cms",
	postCatogary: ['tech', 'business'],
	sessionName: "sid-node-cms",
	sessionSecret: "random string",
};

/* the ret code from back to front: succ, err */

module.exports = config;